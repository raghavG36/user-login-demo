# Optional: Backend API base URL (no trailing slash).
# Leave unset in dev to use Vite proxy (requests to /api → http://localhost:8000).
# Set for production or when frontend is served from another host, e.g.:
# VITE_API_URL=http://localhost:8000
